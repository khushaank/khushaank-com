(() => { const b = document.querySelector('#theme-toggle'); if (!b) return; const set = value => { document.documentElement.dataset.theme = value; b.textContent = `Theme: ${value}`; }; set(localStorage.getItem('theme') || 'auto'); b.addEventListener('click', () => { const next = {auto: 'light', light: 'dark', dark: 'auto'}[document.documentElement.dataset.theme] || 'auto'; localStorage.setItem('theme', next); set(next); }); })();
